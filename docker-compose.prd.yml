services:
    backend:
      build: .
      image: ghcr.io/yakruay/int222-backend:prd
      volumes:
        - ./backendImages:/src/images

networks:
    default:
        external:
            name: int222-network